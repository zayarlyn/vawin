{
	"name": "yy",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "conc -n 'backend,apps > store' 'cd backend && npm run dev' 'cd apps/store && npm run dev' 'cd apps/store && npm run graphql-codegen'"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"concurrently": "^8.2.2"
	}
}
